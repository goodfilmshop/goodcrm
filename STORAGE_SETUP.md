# หน้า Storage

เปิด **ตั้งค่าระบบ → พื้นที่จัดเก็บไฟล์** ด้วยบัญชีผู้ดูแลระบบ
หน้านี้อ่านข้อมูลผ่าน session ผู้ใช้และ Storage RLS เดิม ไม่ใช้ service-role key
แสดงขนาดไฟล์ ชื่อไฟล์ โฟลเดอร์ หมวด วันที่แก้ไข และเปิดไฟล์ผ่านลิงก์ดาวน์โหลดอายุ 60 วินาที
ค้นหา กรองหมวด และแบ่งหน้าครั้งละ 50 ไฟล์ได้

## โควตาพื้นที่

สำหรับโปรเจกต์ goodcrm ปัจจุบัน ระบบใช้ `storage-plan.json` ซึ่งตรวจสอบแพ็กเกจ Free
จาก Supabase เมื่อ 13 กันยายน 2026: โควตา 1 GB (1,000,000,000 bytes)
RPC `crm_storage_usage_summary` อ่านยอดรวมทุกไฟล์ในโปรเจกต์แบบสด เฉพาะ admin ที่ active
โดยไม่ให้สิทธิ์เปิดอ่านไฟล์เพิ่ม แสดงยอดไฟล์ทั้งหมดแยกจากรายการไฟล์ที่เปิดดูได้
พื้นที่คงเหลือคือโควตาลบขนาดไฟล์ปัจจุบัน ไม่ใช่ยอดเฉลี่ยรายรอบบิลของ Supabase
เมื่อต้องเปลี่ยนแพ็กเกจหรือเพิ่มโปรเจกต์ในองค์กร ต้องตรวจสอบและปรับไฟล์นี้ใหม่
ข้อมูลแพ็กเกจไม่ได้ซิงค์อัตโนมัติ หน่วยแสดงผลเป็นฐาน 1000 (KB/MB/GB)
สำหรับโปรเจกต์อื่นที่ไม่ตรงกับไฟล์ดังกล่าว จึงใช้การตั้งค่าโควตาด้านล่าง

กำหนด `CRM_STORAGE_QUOTA_BYTES` ใน environment ของเซิร์ฟเวอร์ แล้วรีสตาร์ต
ค่าเป็นจำนวนเต็มบวก หน่วย bytes ตัวอย่างโควตา CRM 1 GiB คือ `1073741824`
ค่านี้คือพื้นที่ที่จัดสรรให้ CRM ไม่ใช่โควตาแพ็กเกจ Supabase ที่ระบบตรวจพบอัตโนมัติ
หากไม่ได้กำหนด จะยังแสดงพื้นที่ใช้ไป แต่พื้นที่คงเหลือแสดงว่า “ยังคำนวณไม่ได้”
หากเกินโควตา จะแสดงพื้นที่คงเหลือ 0 และจำนวนพื้นที่ที่เกิน
โควตานี้ใช้แสดงผล ไม่ได้บังคับจำกัดการอัปโหลด

`CRM_STORAGE_BUCKETS` ระบุหมวดที่นำมาคำนวณ คั่นด้วย comma
ค่าเริ่มต้นคือ `announcement-images,quotation-images` ตามหมวดที่ CRM ใช้อยู่
การคำนวณครอบคลุมเฉพาะไฟล์ที่ RLS อนุญาตให้อ่านในหมวดเหล่านี้ รวมโฟลเดอร์ย่อย
ไม่รวมฐานข้อมูล ไฟล์นอกหมวด หรือไฟล์ของโปรเจกต์อื่น

ถ้าอ่านหมวดใดไม่ได้ ระบบจะแสดงข้อผิดพลาดแทนยอดรวมที่ขาดข้อมูล
หากมีไฟล์ที่ไม่ทราบขนาด จะไม่คำนวณพื้นที่คงเหลือ
การสแกนจำกัดที่ 2,000 คำขอ เพื่อไม่ให้คำขอทำงานไม่สิ้นสุดกับ Storage ขนาดใหญ่มาก
